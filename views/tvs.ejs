<%- contentFor("body") %>
<div class="container marketing mt-5">
<div class="row">
  <% tvs.forEach(function(tvs){ %>     
  <div class="col-lg-4">
    <img
      class="bd-placeholder-img"
      width="140"
      height="140"
      role="img"
      aria-label="Placeholder: 140x140"
      preserveAspectRatio="xMidYMid slice"
      focusable="false"
      src="<%= tvs.imageUrl %>"
    >

    <h2 class="fw-normal"><%= tvs.name %></h2>
    <p>
      <%= tvs.description %>
    </p>
    <p>
      <a class="btn btn-secondary" href="#"><%= tvs.price.toLocaleString('pt-BR', { style: 'currency', 'currency': 'BRL', }); %> &raquo;</a>
    </p>
  </div>
  <% }) %>
</div>
